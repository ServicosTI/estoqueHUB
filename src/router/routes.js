const { Router } = require("express");
const { requestFlowController } = require("../controller/requesFlowController");

const router = Router();

// router.post("/request-products", requestFlowController);

module.exports = { router };
